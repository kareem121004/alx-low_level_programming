#include "main.h"

/**
 * mul - multiply two integers and returns the result
 * @a: int to be multimplied to b
 * @b: int to be multimplied to a
 * Return: product of a and b
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
