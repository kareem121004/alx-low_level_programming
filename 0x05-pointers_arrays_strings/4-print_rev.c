#include "main.h"

/**
 * print_rev - returns the length of a string
 * @s: is the int to be checked
*/

void print_rev(char *s)
{
	int i;

	while (str[i])
		i++;
	while (i--)
		_putchar(str[i]);

	_putchar('\n);
}
